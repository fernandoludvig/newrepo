<!-- Set title if exists -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
        res.redirect('/')
    } %>
    
    <!-- Message output -->
    <%# messages() %>
    
    <!-- main view content output -->
    <%- grid %>